namespace ConsoleGame.Plugin.Audio;

// Legacy Pure.DI root placeholder retained to avoid build failures after migrating to Splat.
// The actual composition logic now lives in AudioComposition.
internal static class AudioRoot
{
}
